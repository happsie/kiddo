import { Title } from "@core/components/typography/Typography"
import { Color } from "@core/utils/colors"

export const History = () => {
    return <Title style="pacifico" color={Color.LightText}>History</Title>
}