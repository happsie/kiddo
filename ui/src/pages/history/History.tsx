import { Title } from "@components/typography/Typography"
import { Color } from "../../utils/colors"

export const History = () => {
    return (
        <Title color={Color.TextDark}>History</Title>
    )
}
